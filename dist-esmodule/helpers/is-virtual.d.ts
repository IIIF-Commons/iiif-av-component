import { VirtualCanvas } from '../elements/virtual-canvas';
import { Canvas } from 'manifesto.js';
export declare function isVirtual(canvas: Canvas | VirtualCanvas | undefined): canvas is VirtualCanvas;
